# Changes, Setup & Deployment Guide

**Scope:** everything in the working tree vs. commit `8889087` ("fix: make API startup resilient…").
**Date:** 2026-07-11

This document covers: (1) every change and *why* it was made, (2) how to run locally (with and without Clerk), (3) how to deploy to Railway from source, (4) how the admin role works, (5) coverage of the two problem statements, (6) what was tested.

---

## 1. TL;DR

- **Clerk authentication** added end-to-end (invite-only, 10k MAU free tier). The old shared-password mode still works when Clerk env vars are absent — nothing breaks for plain local dev.
- **Admin role**: `info@ahumlabs.com` (Clerk `publicMetadata.role = "admin"`) is the only account that sees/uses **Models** (`/settings/api-keys`), **Transformations**, and **Advanced** (embedding rebuild). Enforced in the API with 403s, not just hidden in the UI.
- **Excel summary report** (problem statement 1): new button on the **Sources** page downloads an `.xlsx` with one row per document (title, type, file/URL, topics, AI summary, notebooks, dates). Also available per-notebook via the API.
- **Add Source wizard fixed**: clicking a notebook in the dropdown now registers; "Create notebook …" works (root cause: Radix `Popover modal={false}` inside a modal Dialog swallows clicks — [shadcn-ui/ui#9712](https://github.com/shadcn-ui/ui/issues/9712)).
- All locale files re-synced (16 missing keys translated into 13 languages, 15 dead keys removed) — the full frontend test suite (57 tests) and locale-parity tests now pass.

---

## 2. What changed and why

### 2.1 Authentication — Clerk (new, this session)

**Why:** the app previously had a single shared password (`OPEN_NOTEBOOK_PASSWORD`) — no user identities, no roles, flagged "insecure, dev-only" in the project docs. Deploying publicly requires real login and an admin/user split. Clerk was chosen for its generous free tier (10,000 MAU), drop-in Next.js components, and easy JWT verification from FastAPI.

**Design principle:** *Clerk mode activates only when Clerk env vars are present.* Without them everything behaves exactly as before (password or no auth). This keeps local dev and upstream compatibility intact. Password-mode behavior is regression-tested.

| File | Change | Reason |
|---|---|---|
| `api/auth.py` | `PasswordAuthMiddleware` now verifies Clerk RS256 JWTs (via JWKS, cached `PyJWKClient`) when `CLERK_ISSUER`/`CLERK_JWKS_URL` is set; stores `{id, email, role}` on `request.state.user`. New `require_admin` FastAPI dependency (403 for non-admins; allows all in password mode). New `get_auth_mode()`. | Single middleware keeps one auth chokepoint; JWKS verification means the API never talks to Clerk per-request (keys cached 1h). |
| `api/routers/auth.py` | `/api/auth/status` now returns `mode: "clerk" \| "password" \| "none"`. | Frontend needs to know which login UI to render. |
| `api/routers/credentials.py` | **All** routes admin-only (router-level dependency). | This is the "Models" page backend — it holds provider API keys for the whole deployment. |
| `api/routers/models.py` | Mutations admin-only (create/delete/test/sync/defaults/auto-assign). `GET /models` etc. stay open because the chat model selector needs them for every user. | Users may *pick* a model for a chat, only admins may *configure* models. |
| `api/routers/transformations.py` | Mutations + `/execute` admin-only; `GET` list stays open (the Add Source wizard shows transformations to everyone). | Same read-vs-configure split. |
| `api/routers/embedding_rebuild.py` | All routes admin-only. | Rebuild is a deployment-wide ("all accounts") operation on the Advanced page. |
| `pyproject.toml` | + `pyjwt[crypto]`, + `openpyxl` as direct deps. | JWT verification; Excel export. |
| `frontend/src/lib/auth/clerk.ts` *(new)* | `isClerkEnabled` build-time flag + `getClerkToken()`. | One switch used everywhere. |
| `frontend/src/components/providers/AuthProvider.tsx` *(new)* | Wraps children in `<ClerkProvider>` only when configured. | `ClerkProvider` throws without a publishable key; password-mode builds must not crash. |
| `frontend/src/app/layout.tsx` | Uses `AuthProvider` (outermost). | Clerk hooks need the provider above everything. |
| `frontend/src/proxy.ts` | `clerkMiddleware()` protects all pages (redirects signed-out visitors to `/sign-in`); keeps the existing `/` → `/notebooks` redirect; falls back to the old behavior in password mode. | Server-side route protection — pages are unreachable without a session, not merely hidden. |
| `frontend/src/app/sign-in/…`, `sign-up/…` *(new, generated by `clerk init`)* | Clerk-hosted sign-in/sign-up pages. | Standard Clerk App Router setup. |
| `frontend/src/app/(auth)/login/page.tsx` | Redirects to `/sign-in` in clerk mode; renders the old password form otherwise. | One login entry point per mode. |
| `frontend/src/lib/api/client.ts` | Request interceptor attaches `Bearer <Clerk session JWT>` (auto-refreshed — Clerk tokens live ~60 s); 401s redirect to the active login page. | Same header shape as before, so the FastAPI middleware and the Next SSE proxy needed no changes. |
| `frontend/src/lib/hooks/use-auth.ts` | Split into a Clerk adapter (session state from Clerk, logout via `signOut()`) and the legacy password hook; chosen once at build time. | Components (`AppSidebar`, dashboard layout, `LoginForm`) keep the exact same `useAuth()` contract. |
| `tests/test_auth_clerk.py` *(new)* | 13 offline tests: signs JWTs with a throwaway RSA key, stubs JWKS. Valid/expired/garbage/wrong-issuer tokens; admin 200 vs user 403; password-mode passthrough. | Proves the enforcement matrix without a live Clerk instance. |
| `tests/conftest.py` | Blanks `CLERK_ISSUER`/`CLERK_JWKS_URL` for the test run. | A developer's `.env` (which now sets `CLERK_ISSUER`) must not flip the whole test suite into clerk mode. |

### 2.2 Admin-only UI gating (new, this session)

**Why:** you asked for all pages visible to users **except** Models, Transformations, Advanced — editable only by the admin login. UI hiding is convenience; the API 403s above are the real boundary.

| File | Change |
|---|---|
| `frontend/src/lib/hooks/use-is-admin.ts` *(new)* | `useIsAdmin()` — reads Clerk `publicMetadata.role === 'admin'`; returns `true` in password mode (single operator). |
| `frontend/src/components/auth/AdminGuard.tsx` *(new)* | Wraps admin pages; redirects non-admins to `/notebooks` with an "Administrator access required" toast. Content (and its API queries) never mounts for non-admins. |
| `frontend/src/app/(dashboard)/settings/api-keys/page.tsx`, `transformations/page.tsx`, `advanced/page.tsx` | Wrapped in `AdminGuard`. |
| `frontend/src/components/layout/AppSidebar.tsx` | Models / Transformations / Advanced nav items marked `adminOnly` and filtered for non-admins. |
| `frontend/src/components/common/CommandPalette.tsx` | Same three destinations hidden from ⌘K for non-admins. |
| `frontend/src/components/layout/SetupBanner.tsx` | Only rendered for admins — its credential-status queries hit now-admin-only endpoints, and a "configure your keys" banner is useless to users who can't. |
| All 14 locale files | New `common.adminOnly` key, translated. |

### 2.3 Excel summary report — problem statement 1 (new, this session)

**Why:** PS1 asks for a quick summary sheet of a bulky folder of PDFs/Word files so files can be revisited later. Per-source AI summaries already exist (insights generated by the default "summarize" transformation at ingest); what was missing was the **consolidated Excel export**.

| File | Change |
|---|---|
| `api/routers/export.py` *(new)* | `GET /api/export/summary-report[?notebook_id=…]` → streams an `.xlsx` (openpyxl): Title, Type (File/Link/Text), File/URL, Topics, Summary, Notebooks, Added, Updated. Summary preference: summary-type insights → any insights → 500-char excerpt of extracted text. Batched queries (3 total, no per-source round trips); illegal-XML chars stripped; 404 for unknown notebook ids. |
| `api/main.py` | Router registered under `/api/export`. |
| `frontend/src/lib/api/export.ts` *(new)* | Blob download through the authed API client (a plain `<a href>` couldn't send the Authorization header). |
| `frontend/src/app/(dashboard)/sources/page.tsx` | "Export Excel summary" button (spreadsheet icon, spinner while generating, success/error toasts). |
| All locale files | `sources.exportSummary`, `exportSummarySuccess`, `exportSummaryError`, translated. |

**Verified live** against the real database: 22 sources exported with insight summaries and notebook names; notebook-scoped export and 404 case verified; the endpoint itself requires auth in clerk mode.

### 2.4 Add Source wizard — notebook dropdown fix (this session's earlier work)

**Why (root cause):** the notebook picker was `<Popover modal={false}>` rendered inside the modal Add Source `Dialog`. Radix modal dialogs set `pointer-events: none` on `<body>`; the popover portals into `<body>`, and with `modal={false}` Radix never re-enables pointer events — so the dropdown rendered but **every click silently did nothing** (including "Create notebook …"). Known upstream bug: shadcn-ui/ui#9712.

**Fix (`frontend/src/components/sources/steps/NotebooksStep.tsx`):** `modal={true}` on the popover, plus the multi-select combobox UX you specified: selected notebooks show as removable chips in the search bar, typing filters the list with the matched text bolded, and "Create notebook \"…\"" creates + selects in place.

### 2.5 Changes inherited from earlier working sessions (already in the tree, kept)

These were uncommitted before this session started; documented here so the diff is fully accounted for:

- **Home = Ask & Search** (`(dashboard)/page.tsx` redirects to `/search`; `search/page.tsx` redesigned with mode hints, filter popover, example prompts, empty-knowledge-base state). *Why:* makes the Q&A experience the landing page — this is PS2's primary surface.
- **Vision model support** (`open_notebook/graphs/source.py` +157 lines, `ai/models.py`, `api/models.py`, `types/models.ts`, api-keys page): standalone images (PNG/JPG/BMP/TIFF…) are routed through a vision-capable chat model for OCR, with format normalization to PNG where providers are picky; new `default_vision_model` default (falls back to the chat model). *Why:* content-core only OCRs images embedded in documents, not standalone image files.
- **Podcasts page simplification** (−72/+11) and removal of podcasts from the sidebar; **branding** (`layout.tsx` title → "IRS Knowledge base"); **`use-sources` additions** (+19, recently-viewed support); minor `SourceTypeStep`/`LoginForm` copy tweaks; `docs/3-USER-GUIDE/adding-sources.md` touch-up.

### 2.6 i18n & test hygiene (this session)

- **Locale parity restored:** 16 keys that existed only in en-US (`navigation.home`, `searchPage.*` incl. `examplePrompts`, `models.visionModel*`, `notebooks.createNewNamed`) were translated and added to all 13 other locales; 15 orphaned keys (old podcasts/search UI) removed from all 14. The locale test-suite requirement is: every key in every language, every key referenced in code.
- `search/page.tsx` example prompts now use full key paths (`searchPage.examplePrompts.certRequirements`) so static analysis can see them.
- `tests/test_auth_clerk.py` added (see 2.1).

### 2.7 Upstream update notifications removed (2026-07-13, user request)

- Deleted `use-version-check.ts` and its call in `(dashboard)/layout.tsx` — the "Version X.Y.Z Available / View on GitHub" toast no longer appears anywhere.
- `advanced/components/SystemInfo.tsx` now shows only the current running version; the "Latest Version", "Update available" badge, GitHub link, and update-check-failed note were removed.
- `lib/config.ts` / `lib/types/config.ts` no longer carry `latestVersion`/`hasUpdate`; 7 orphaned `advanced.*` i18n keys removed from all 14 locales (locale tests pass).
- Reason: this is a branded internal deployment ("IRS Knowledge base"); upstream Open Notebook release notices are irrelevant noise for its users.

### 2.8 Invite links pinned to the deployed URL (2026-07-14, deployment fix)

- **Problem:** invite emails redirected users to `http://localhost:3000` instead of the Railway app. Cause: the admin panel sends `redirect_url = window.location.origin + '/sign-up'`, so invites created from a localhost admin session baked localhost into the Clerk invitation. Because Clerk sign-ups are **restricted** (invite-only), a user who then manually opened the production `/sign-up` had no invitation ticket (`__clerk_ticket`) in the URL — Clerk therefore blocked every sign-up method, including "Continue with Google".
- **Fix:** `api/routers/admin.py` now honors a `PUBLIC_APP_URL` env var: when set, every invitation's redirect becomes `{PUBLIC_APP_URL}/sign-up` regardless of which origin the admin was browsing from. Unset (local dev), the browser origin is used as before.
- **Operator steps after deploying this fix:** set `PUBLIC_APP_URL` on the Railway service (see §5 table), **revoke all pending invitations** in `/admin` (they permanently embed the old localhost redirect) and re-send them. Also set the Clerk application's Home URL to the deployed domain (Clerk Dashboard → NavCert → **Configure → Account Portal / Paths**) so any Account-Portal fallback also lands on production.
- **Google sign-up for invitees:** from the ticketed invite link, email+password sign-up works directly; "Continue with Google" works only when the Google account's email exactly matches the invited address. Once the account exists, later *sign-ins* with Google link automatically by verified email.

---

## 3. Clerk: what is already configured (via Clerk CLI)

The Clerk application is **NavCert** (`app_3BbgbBY7qjpsvEH1uZMiyorRLIV`), development instance `proper-sponge-22.clerk.accounts.dev`. Already done — you don't need to repeat any of this:

1. `@clerk/nextjs` installed; `clerk init` scaffolding applied; keys pulled into `frontend/.env.local` (`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, sign-in/up URLs).
2. `CLERK_ISSUER=https://proper-sponge-22.clerk.accounts.dev` appended to the root `.env` (this is what flips the **API** into clerk mode).
3. Instance config patched: **sign-up mode = restricted** (invite-only), **phone number no longer required** (email + password only), **session token claims** now include `role` (from `publicMetadata.role`) and `email`.
4. Users created:
   - `info@ahumlabs.com` (email changed from admin@ahumai.co.in on 2026-07-13) — `publicMetadata: {"role": "admin"}` — password as you specified. ⚠️ **That password appeared in our chat; consider rotating it in the Clerk dashboard (Users → admin → Change password) before anything public.**
   - `testuser@ahumai.co.in` — regular (no role) test user, password `Xk9#mPv2!wQz7Lrt`. Delete after testing.

**Day-to-day admin tasks** (Clerk dashboard → https://dashboard.clerk.com, or `clerk users` CLI):
- **Invite a user:** Users → Create user (sign-up is closed to the public).
- **Make someone an admin:** Users → select user → Metadata → Public → `{"role": "admin"}`. Takes effect on their next token refresh (≤60 s).
- **Remove access:** delete/ban the user in Clerk.

---

## 4. Running locally

### Clerk mode (what's configured right now)
```bash
# 1. SurrealDB (Docker)          — already running as open-notebook-surrealdb-1
docker compose up surrealdb -d
# 2. API (reads CLERK_ISSUER from .env → clerk mode)
uv run --env-file .env uvicorn api.main:app --host 0.0.0.0 --port 5055
# 3. Worker
uv run --env-file .env surreal-commands-worker --import-modules commands
# 4. Frontend (reads Clerk keys from frontend/.env.local)
cd frontend && npm run dev
```
Open http://localhost:3000 → redirected to `/sign-in` → log in as `info@ahumlabs.com` (full UI) or `testuser@ahumai.co.in` (no Models/Transformations/Advanced; 403 if forced).

### Password mode (upstream-compatible fallback)
Remove/blank `CLERK_ISSUER` in `.env` and the two Clerk keys in `frontend/.env.local`, restart both. Optionally set `OPEN_NOTEBOOK_PASSWORD`. Everything behaves as before this work.

---

## 5. Deploying to Railway (from source)

> Railway has no permanent free tier (~$5 one-time trial credit, then Hobby ≈ $5/mo). Clerk stays free at this scale.

> **Persistence requires volumes + Hobby plan.** Without a volume on the `surrealdb` service, *every* redeploy/restart of that service wipes all data (models config, credentials, notebooks, users' notes). Trial-plan volumes are capped at 0.5 GB and are **deleted 30 days after the trial credit expires**, so upgrade to Hobby (5 GB volumes, no expiry) before relying on it. Also keep `OPEN_NOTEBOOK_ENCRYPTION_KEY` stable forever — changing it makes stored provider API keys undecryptable.

### Service 1 — `surrealdb`
- **Deploy from Docker image:** `surrealdb/surrealdb:v2`
- **Start command:** `surreal start --log info --user $SURREAL_USER --pass $SURREAL_PASSWORD rocksdb:/mydata/mydatabase.db`
- **Volume:** mount at `/mydata`. **No public domain** (private networking only).
- **Variables:** `SURREAL_USER`, `SURREAL_PASSWORD` (strong random values).

### Service 2 — `open-notebook` (this repo)
- **Deploy from GitHub repo.** Railway auto-detects the root `Dockerfile` (API + worker + frontend under supervisord — no changes needed).
- **Volume:** mount at `/app/data` (uploads, podcasts, LangGraph checkpoints).
- **Networking:** generate a domain, set **target port 8502**. The API (5055) stays private; the browser reaches it through Next.js's `/api/*` rewrite proxy.
- **Variables:**

| Variable | Value / note |
|---|---|
| `SURREAL_URL` | `ws://surrealdb.railway.internal:8000/rpc` |
| `SURREAL_USER` / `SURREAL_PASSWORD` | same as service 1 (use shared variables) |
| `SURREAL_NAMESPACE` / `SURREAL_DATABASE` | `open_notebook` / `open_notebook` |
| `OPEN_NOTEBOOK_ENCRYPTION_KEY` | long random secret (encrypts stored provider keys) |
| `API_URL` | `https://<your-railway-domain>` — **no port**. Critical: without it the frontend auto-detects and wrongly appends `:5055`. |
| `INTERNAL_API_URL` | `http://localhost:5055` (default, explicit for clarity) |
| AI provider keys | same `OPENAI_API_KEY`/`GEMINI_API_KEY`/… you use locally (or configure in the admin Models page after deploy) |
| `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` | Clerk publishable key. **NEXT_PUBLIC_ vars are inlined at build time** — the root `Dockerfile` now declares this (and the sign-in/up URL vars) as `ARG`, so setting it as a Railway service variable makes Railway pass it into the build automatically. Without it the frontend ships with Clerk disabled. |
| `CLERK_SECRET_KEY` | Clerk secret (server SDK) |
| `CLERK_ISSUER` | your Clerk instance issuer URL |
| `PUBLIC_APP_URL` | `https://<your-railway-domain>` — pins invite-email redirects to the deployed app even when invites are sent from a localhost admin session (§2.8). |
| *(leave `OPEN_NOTEBOOK_PASSWORD` unset)* | password mode off in clerk mode |

### Clerk production notes
- The current keys are the **development instance** (works immediately, shows a Clerk dev banner, 100-user cap). For real use, create the **production instance** (Clerk dashboard → "Deploy to production", needs a domain, e.g. `notebook.ahumai.co.in`) and swap the three Clerk vars for the `pk_live_/sk_live_` versions + production issuer. Re-apply: restricted sign-ups, session claims (`role`, `email`), the admin user — same steps as §3 (or `clerk config pull/patch --instance prod`).
- After deploy: check Railway logs for `Authentication mode: Clerk (JWT verification)` and successful migrations; confirm `https://<domain>/api/notebooks` returns **401** without a token.

---

## 6. Problem-statement coverage

**PS1 — summary document (Excel) for a folder of PDFs/Word files: ✅ now complete.**
Upload the folder's files via Add Source (batch upload supports up to 50 files at once; the default "summarize" transformation runs at ingest). Then **Sources page → "Export Excel summary"** downloads the spreadsheet (or `GET /api/export/summary-report?notebook_id=…` for one notebook). This was the missing piece; everything else existed.

**PS2 — local search over a folder of documents + technical Q&A with references: ✅ already existed; now the app's home page.**
The **Ask & Search** page (`/search`, where the app now lands) has both modes: *Search* (full-text + vector search over sources/notes) and *Ask* (LangGraph `ask` workflow: retrieves relevant passages by embedding similarity, synthesizes an answer, and **cites the source documents** it drew from, linked back to each source). Individual answers can be saved as notes. Nothing was missing here; verify with a few technical queries against your corpus after deploy.

---

## 7. What was verified

- **Backend:** 233 passed, 2 failed — the 2 failures are **pre-existing Windows-only** path-separator assertions in `tests/test_podcast_path.py` (compare POSIX strings on Windows), unrelated to this work. Includes 13 new Clerk auth tests (valid/expired/garbage/wrong-issuer JWTs; admin 200 vs user 403; password-mode passthrough).
- **Frontend:** `tsc --noEmit` clean; ESLint 0 errors (8 pre-existing warnings); **57/57 vitest tests pass** including locale parity + unused-key detection (both failing before this session).
- **Live, clerk mode:** API reports `mode: "clerk"`; `/api/notebooks` → 401 without/with garbage token; frontend `/` → `/notebooks` → `/sign-in` redirect chain for signed-out visitors; sign-in page renders.
- **Live, export:** real `.xlsx` generated from the current database (22 sources, summaries from insights, notebook names, correct filename); notebook-scoped export; 404 on bad notebook id; endpoint 401 without auth.
- **Not verified by me:** an interactive browser login (requires a human; the permission sandbox rightly refused to mint live session tokens). First thing to try when you're back: log in at http://localhost:3000 as admin and as `testuser@ahumai.co.in` and click around.

## 8. Known notes / follow-ups

1. **Rotate the admin password** in Clerk (it was pasted in chat) and delete `testuser@ahumai.co.in` when done testing.
2. The 2 Windows-only pytest failures and 8 ESLint warnings predate this work; harmless but tidy-up candidates.
3. `api_log.txt` / `frontend_log.txt` / `worker_log.txt` / `api_clerk.log` are local dev-server logs — don't commit them.
4. In clerk mode the **shared workspace** model applies: all invited users see the same notebooks/sources (per your decision); only the three admin pages are restricted.
5. For production, set `CORS_ORIGINS=https://<your-domain>` on the API (it currently warns about the `*` default).
